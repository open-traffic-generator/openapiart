components:
  schemas:
    Config:
      type: object
      properties:
        a:
          type: string
        b:
          type: number
        c:
          type: integer
        d:
          type: array
          items:
            type: string
            enum: [a, b, c]
        