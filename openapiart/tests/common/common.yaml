components:
  schemas:
    GlobalObject:
      type: object 
      properties:
        name:
          type: string
          format: global-unique
    ListObject:
      type: object
      properties:
        name:
          type: string
          format: list-unique
  responses:
    Success:
      '200':
        description: 'Success with payload'
